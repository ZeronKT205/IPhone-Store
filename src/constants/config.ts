export const LOW_STOCK_THRESHOLD = 5;

export const ORDER_CODE_PREFIX = {
  REPAIR: "SC",
  SALES: "SO",
} as const;

export const DEFAULT_WARRANTY_FEE = 0;

// Số ký tự tối đa hiển thị của description trước khi truncate + nút "Xem thêm"
export const DESCRIPTION_PREVIEW_CHARS = 80;
